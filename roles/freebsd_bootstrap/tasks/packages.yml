---
- name: Install base packages
  community.general.pkgng:
    name: "{{ item }}"
  with_items:
    - rsync
    - py311-certbot-nginx
    - tree
    - rust
    - openjpeg
    - cmake-core
    - gobject-introspection
    - cairo
    - pango
    - webfonts
